export enum DealCategory {
  Discount = 'discount',
  Gift = 'gift',
  Other = 'other',
}

export enum DealDiscountType {
  Percentage = 'percentage',
  Fixed = 'fixed',
  Free = 'free',
}

export enum DealType {
  Active = 'active',
  Passive = 'passive',
}
